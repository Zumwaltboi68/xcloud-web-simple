{
  "name": "xcloud-web",
  "version": "0.3.0",
  "description": "A multi-user web client for Xbox Cloud Gaming, built directly on unknownskl/xbox-xcloud-player's own server+library (verified against its actual source, not guessed). Each app account gets its own isolated Xbox session.",
  "main": "server/index.js",
  "private": true,
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "xal-node": "^1.1.2",
    "xbox-xcloud-player": "^1.0.0-beta7"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "nodemon": "^3.0.1"
  }
}
